تست فوری یک‌ساعته دیدبان ولایت

این تست مستقل از کنداکتور اصلی است و به محض زدن Run workflow شروع می‌شود.

فایل‌ها:
- test_one_hour_now.py -> ریشه Repository
- .github/workflows/test-one-hour-now.yml -> مسیر Workflow
- requirements.txt -> در صورت نیاز جایگزین نسخه فعلی

بعد:
Actions > Test One Hour Now > Run workflow

مدت ضبط:
3595 ثانیه، حدود 59 دقیقه و 55 ثانیه.

خروجی:
- متن کامل در Artifact
- تحلیل در Google Sheet
- گزارش در بله

این Workflow زمان‌بندی اصلی Velayat Live Monitor را تغییر نمی‌دهد.
