# Bale Bot Test

Files:
- `test_bale.py` -> repository root
- `.github/workflows/test-bale.yml` -> workflow directory

Required GitHub repository secrets:
- `BALE_BOT_TOKEN`
- `BALE_CHAT_ID`

Then run **Test Bale Bot** from GitHub Actions.
